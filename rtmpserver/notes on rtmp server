https://github.com/illuspas/Node-Media-Server
here is the github link for this server set up

find usage 
npm version (recommended) read this and use the example really easy

change package.json test to 

"scripts": {
    "start": "node index.js"
  },

  than npm start to run the server 
  u should see node media serer v2.2.4
  ports 1935 for send 8000 for consumption

  obsproject.com 
  select ur system and install OBS 


https://www.npmjs.com/package/flv.js
Getting started 

<script src="flv.min.js"></script>
<video id="videoElement"></video>
<script>
    if (flvjs.isSupported()) {
        var videoElement = document.getElementById('videoElement');
        var flvPlayer = flvjs.createPlayer({
            type: 'flv',
            url: 'http://example.com/flv/video.flv'
        });
        flvPlayer.attachMediaElement(videoElement);
        flvPlayer.load();
        flvPlayer.play();
    }
</script> 